export enum NodeType {
    BASIC_BLOCK,
    CLASS,
    METHOD,
    FIELD,
    FILE,
    INSTRUCTION,
    METADATA,
    PACKAGE,
    SWITCH_STMT,
    SYSCALL,
    TAG_CATEGORY,
    CATCH_STMT,
    SWITCH_CASE,
    FUNC,
    EXEC_SECTION,
    VAR
}